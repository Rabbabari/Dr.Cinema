import { StyleSheet } from "react-native";

export default StyleSheet.create({
	listContainer: {
		flex: 1,
		flexDirection: "row",
		backgroundColor: "white",
		padding: 10,
		paddingBottom: 20,
	},
});
